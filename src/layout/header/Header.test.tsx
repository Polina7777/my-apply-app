// import { BrowserRouter } from 'react-router-dom';
// import { fireEvent, render, screen } from '@testing-library/react';
// import Header from './Header';

// describe('find link element', () => {
//   test('find link element', () => {
//     render(<Header />, { wrapper: BrowserRouter });
//     const linkElement = screen.getByText(/About us/i);
//     expect(linkElement).toBeInTheDocument();
//   });
// });

// test('should navigate to about-us-page when link is clicked', async () => {
//   render(<Header />, { wrapper: BrowserRouter });
//   const linkElement = screen.getByLabelText('Link');
//   fireEvent.click(linkElement);
//   expect(window.location.href).toBe('http://localhost/about-us');
// });
